apiVersion: origami/v1
kind: Schematic
metadata:
  name: origami-er
  module: github.com/dpopsuev/origami-er
  namespace: er
  description: "Entity Resolution — LLM-driven matching across RP, Jira, GitHub"
spec:
  version: "1.0.0"
  hooks: "Hooks()"
  resolver: SchematicResolver
